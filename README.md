# Telegram-бот для рекомендаций вакансий

Этот проект реализует Telegram-бота, который предлагает пользователю вакансии каждые 5 минут. Вакансии выбираются случайным образом из базы данных PostgreSQL, а пользователь может оценить каждую вакансию с помощью кнопок с эмодзи (👍, 👎, ➖). Если все вакансии оценены, бот уведомляет: «Больше нет доступных вакансий. Ждите новых обновлений.»

## Содержимое проекта

- **Бот:** Основной код бота находится в `bot/main.py`. В этом файле реализована логика отправки вакансий и обработки пользовательских оценок.
- **База данных:** Используется PostgreSQL для хранения вакансий и оценок пользователей. Модели описаны в `database/models.py`, а подключение и создание сессий – в `database/db.py`.
- **Загрузка вакансий:** Скрипт `database/fetch_vacancies.py` загружает вакансии с API HH.ru и сохраняет их в базу данных.
- **Миграции:** Управление схемой базы данных осуществляется с помощью Alembic (конфигурация в `alembic.ini`, файлы миграций в `migrations`).
- **Docker:** Проект полностью контейнеризирован и может быть запущен через `Dockerfile` и `docker-compose.yml`.
- **Скрипты:** В проекте есть `entrypoint.sh`, отвечающий за инициализацию базы данных, применение миграций и запуск бота.
- **Файл зависимостей:** В `requirements.txt` указаны все необходимые библиотеки для работы проекта.
- **.gitignore:** Файл `.gitignore` содержит исключения для временных файлов, виртуального окружения и конфигурационных данных.

## Требования

Для запуска проекта вам потребуется:

- **Docker и Docker Compose** (рекомендуется)
- Или:
  - Python 3.12.3
  - PostgreSQL 16.4
  - pip 24.0

## Запуск проекта в Docker (рекомендуемый способ)

### 1. Подготовка

1. Убедитесь, что у вас установлены **Docker** и **Docker Compose**.
2. Склонируйте репозиторий проекта:
   ```bash
   git clone <URL_репозитория>
   cd <название_папки>
   ```
3. Создайте `.env` файл в корне проекта и укажите в нем переменные окружения:
   ```env
   TOKEN=<ваш_телеграм_бот_токен>
   DATABASE_URL=postgresql://job_user:dsNSMm283290nc@db/job_bot
   ```

### 2. Запуск контейнеров

Выполните команду:
```bash
docker-compose up --build
```

При запуске:
- Контейнер `db` создаст базу данных `job_bot` и пользователя `job_user` с нужными правами.
- Контейнер `bot` выполнит `entrypoint.sh`, который:
  - Применит миграции Alembic;
  - Запустит загрузку вакансий с HH.ru;
  - Запустит Telegram-бота.

### 3. Использование бота

После запуска бота найдите его в Telegram и отправьте команду `/start`. Бот начнет присылать вам вакансии каждые 5 минут.

## Запуск проекта без Docker

### 1. Установка зависимостей

1. Создайте виртуальное окружение и активируйте его:
   ```bash
   python3 -m venv venv
   source venv/bin/activate  # Для Linux и macOS
   venv\Scripts\activate  # Для Windows
   ```
2. Установите зависимости:
   ```bash
   pip install --upgrade pip
   pip install -r requirements.txt
   ```

### 2. Настройка базы данных

1. Убедитесь, что PostgreSQL установлен и запущен.
2. Создайте базу данных и пользователя:
   ```sql
   CREATE DATABASE job_bot;
   CREATE USER job_user WITH PASSWORD 'dsNSMm283290nc';
   ALTER ROLE job_user SET client_encoding TO 'utf8';
   ALTER ROLE job_user SET default_transaction_isolation TO 'read committed';
   ALTER ROLE job_user SET timezone TO 'UTC';
   GRANT ALL PRIVILEGES ON DATABASE job_bot TO job_user;
   ```
3. Добавьте в `.env` файл данные подключения:
   ```env
   TOKEN=<ваш_телеграм_бот_токен>
   DATABASE_URL=postgresql://job_user:dsNSMm283290nc@localhost/job_bot
   ```

### 3. Применение миграций и загрузка вакансий

1. Примените миграции Alembic:
   ```bash
   alembic upgrade head
   ```
2. Запустите загрузку вакансий в базу данных:
   ```bash
   python database/fetch_vacancies.py
   ```

### 4. Запуск бота

Запустите бота командой:
```bash
python -m bot.main
```

## Дополнительная информация

- **Миграции и данные:**
  - Alembic управляет схемой базы данных.
  - Вакансии загружаются с HH.ru с помощью `fetch_vacancies.py`.
- **Логи и отладка:**
  - Логирование осуществляется через `logging`.
  - Для отладки можно проверять содержимое базы с помощью команд PostgreSQL (`SELECT * FROM vacancy;`).
- **Автоматизация и скрипты:**
  - `entrypoint.sh` автоматизирует инициализацию базы данных и запуск бота.
- **Файл `.gitignore`** исключает ненужные файлы из репозитория.

